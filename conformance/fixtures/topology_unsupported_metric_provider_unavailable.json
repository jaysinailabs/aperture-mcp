{
  "case": "topology_unsupported_metric_provider_unavailable",
  "entry": "topology.metric",
  "covers": ["C-TP-4"],
  "negative": false,
  "input": {
    "metric": "bridges",
    "graph": {
      "edges": [
        {"src": "a", "dst": "b", "label": "depends_on"},
        {"src": "b", "dst": "c", "label": "depends_on"}
      ]
    }
  },
  "expected": {
    "status": "provider_unavailable",
    "reason_required": true,
    "topology": {
      "cut_vertices": [],
      "supported_metrics": ["cut_vertices"]
    },
    "assertions": [
      {"path": "/status", "equals": "provider_unavailable"},
      {"path": "/reason", "non_empty": true},
      {"path": "/reason", "contains": "bridges"},
      {"path": "/cut_vertices", "equals": []},
      {"path": "/supported_metrics", "equals": ["cut_vertices"]}
    ]
  }
}
